fib: function integer(i:integer) = {
    if(i == 0)
        return 0;
    if(i == 1)
        return 1;

    return fib(i-1) + fib(i-2);
}

main: function void() = {
    i:integer = fib(20);

    print i, '\n';
}
